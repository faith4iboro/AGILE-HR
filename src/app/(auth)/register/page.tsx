import { RegisterForm } from "@/features/auth/register-form";

export const metadata = { title: "Create your workspace — AuraHR" };

export default function RegisterPage() {
  return <RegisterForm />;
}
